# All settings are optional. Blank nickname/UUID are generated and persisted automatically.
NICKNAME=
UUID=
INTERVAL=1280
HTTP_CONCURRENCY=2
# 0 disables live relay; HTTP collection continues.
LIMIT=5
STATUS_PORT=9464
VERBOSE=false
